apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudclinic-chart.fullname" . }}
  labels:
    {{- include "cloudclinic-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudclinic-chart.selectorLabels" . | nindent 4 }}
